# R6.60.b — 99 个 untracked 文件清理 + .gitignore 加固

**日期**: 2026-09-06  
**作者**: PXX  
**commit**: chore(R6.60.b): cleanup 99 untracked files + tighten .gitignore  
**关联**: R6.60 (commit 8e9376d)  

---

## 背景

R6.59+ zjlab 部署成功后审计 D:\AliCPT git 状态，发现 **99 个 untracked 文件**（加上 R6.60 commit 的 4 个后 = 103 个）混杂在工作树里，包括：
- v4.16 时代的 10 个 docs/*.md（已迁移到 docs/legacy/v4.16/）
- gw-pipeline 完整 src 目录（28+ 个 .py）— **从未 tracked**（R6.55+ 之前可能是 submodule）
- gw-mcp-server 完整目录
- 配置模板：`.env.example`, `.env.template`, `.lighthouserc.json`, `README.md`, `deploy_v438.py`
- 2 个 GitHub Actions 工作流：font-subset.yml, lighthouse.yml
- 1 个孤儿 git submodule：**gw-backend/**（指向已废弃的 GravitationalWaveTeam/GravitationalWave.git）

---

## 本次提交（75 files, +16705 lines）

### 配置与文档
- `.env.example`, `.env.template`, `.lighthouserc.json`, `README.md`, `deploy_v438.py` — 部署/构建基础
- `.github/workflows/font-subset.yml`, `lighthouse.yml` — R6.40 perf budget + 字体子集化

### 历史文档归档
- 10 个 `docs/*-v4.16.md` → 移到 `docs/legacy/v4.16/`（保留可读性 + git history）
- 新增 `docs/incident-response-runbook.md`, `docs/legacy-re-export-sop.md` — 当代 SOP

### gw-pipeline 完整 src 落地
- 28 个核心模块：`ai_metrics.py`, `anomaly_classifier.py`, `audit_mongo.py`, `batch_scheduler.py`,
  `config_manager.py`, `dl_inference.py`, `fits_core.py`, `fits_upload.py`, `job_queue.py`,
  `local_llm.py`, `model_registry.py`, `output_guard.py`, `pipeline_logging.py`,
  `provenance.py`, `secrets_manager.py`, `source_extraction.py`, `thumbnail_cache.py`,
  `tool_cache.py`, `audit_v437_extensions.py` + `routes/{hips,v437-v447}.py`
- 1 个 agent 子包：`agent/{agent_loop, tool_schemas, tools}.py`
- Dockerfile, LICENSE, pyproject.toml, pytest.ini, prometheus.yml
- scripts/download_models.py + benchmark/{benchmark_classifiers, benchmark_rule_classifier}.py
- 5 个 model .json 配置（scaler/architecture，**不含 .onnx 二进制**）
- 4 个 tests（conftest + test_config_manager/test_dl_inference/test_provenance）

### gw-mcp-server 完整落地
- `Dockerfile`, `mcp_server.py`, `server.py`, `tools.py`, `gw_client.py`, `real_data_loader.py`, `requirements.txt`

---

## .gitignore 加固（新增 13 条规则）

```gitignore
# R6.60.b untracked cleanup (2026-09-06)
docker-images/             # 4 .tar files (~GB), not in version control
real-data/                 # test data JSON (no need to commit)
sample_data/               # local dev fixtures
npm-cache/                 # npm cache dir

**/target/                 # Maven/Gradle build artifacts
**/build/
**/__pycache__/
**/*.pyc
**/*.class
**/dist/
**/node_modules/
**/.venv/
**/venv/

gw-pipeline/models/*.onnx  # ~60MB ea, downloaded by scripts/download_models.py
# Keep .json configs (scalers/architectures)
```

---

## ⚠️ 不在本次提交（5 块留待）

### 1. `gw-backend/` (孤儿 git submodule) → **R6.61 处理**
- 本地 `gw-backend/.git/` 指向 `https://github.com/GravitationalWaveTeam/GravitationalWave.git`
- HEAD = `fa13364` (R6.54)，**落后主仓 R6.55+ ~4 个月**
- 没 `.gitmodules` 引用 → 是迁移到 NOBEL-Pxx/gw-platform 时丢失的孤儿
- **R6.61 计划**：
  - (a) 从 NOBEL-Pxx/gw-platform 单独 re-clone（或直接删本地 clone，从新 commit 拉取）
  - (b) 或重新初始化为 proper submodule（git submodule add + .gitmodules entry）
  - 评估 (a) vs (b) 决定后再 commit

### 2. `docker-images/*.tar` (4 文件，~GB) → **永久 .gitignored**
- alicpt-gw-backend.tar / -frontend.tar / -mcp-server.tar / -pipeline.tar
- 应该从对象存储或 docker registry 拉取，不进 git

### 3. `real-data/`, `sample_data/` → **永久 .gitignored**
- 测试数据 + 本地 fixtures

### 4. `npm-cache/` → **永久 .gitignored**

### 5. `gw-pipeline/models/*.onnx` (60MB) → **永久 .gitignored**
- 通过 `scripts/download_models.py` 下载
- model .json (scaler/architecture) **保留**在 git

---

## 验证

```bash
# 期望 untracked 数量从 99 → 4 (仅 gw-backend/ + 可能残留)
git status --short | grep '^??' | wc -l   # 期望: ~4

# 期望所有 R6.60.b 文件已被跟踪
git ls-files | grep -E "(legacy/v4.16|font-subset|mcp_server|\.env\.example)" | wc -l

# gw-pipeline src 完整
ls gw-pipeline/src/pipeline/*.py | wc -l   # 期望: 20+
```

## 影响

- ✅ 工作树 99 → 4 untracked（仅留 gw-backend 孤儿）
- ✅ gw-pipeline 源码正式进入版本控制
- ✅ .gitignore 阻止未来类似的垃圾积累
- ⏳ gw-backend submodule 修复 → R6.61 第一个增量

## 相关

- R6.59+ zjlab Redis 部署：`r659plus-zjlab-deploy`
- R6.60 compose overrides：commit 8e9376d
- R6.61 计划：HiPS CDN 缓存优化 / Aladin 缩略图一致性 / CSP wasm-unsafe-eval 监控 / gw-backend submodule 修复

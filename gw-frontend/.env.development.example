# Mock 配置
# 是否启用 Mock 数据（用于本地开发，当后端接口未就绪时）
VITE_USE_MOCK=false

# 后端服务地址配置
# 开发环境：前后端在不同端口，需要配置后端地址
# 如果后端返回相对路径，前端会使用此地址进行拼接
# 生产环境：通过 nginx 反向代理，不需要配置此变量
VITE_BACKEND_BASE_URL=http://your-backend-host:8093
